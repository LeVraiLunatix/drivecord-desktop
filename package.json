{
  "name": "drivecord-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Client desktop Windows pour Drivecord (drivecord.app)",
  "license": "MIT",
  "scripts": {
    "shell": "node scripts/build-shell.mjs",
    "tauri": "tauri",
    "app:dev": "npm run shell && tauri dev",
    "app:build": "npm run shell && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
